# 버그 분석 보고서 . - 대  이슈

**작성일** --
**분석자**   (-)
**대상 영상**   .
**심각도**  (등급 달성 차단)

---

##  

. 영상에서 발견된 가지 문제를 **근본 원인 수준**에서 분석한 결과, **가지는 설계 결함**, **가지는 데이터 품질 문제**로 분류됩니다.

 버그   문제  분류  근본 원인  점수 손실  수정 난이도 
--------------------------------------------------------
 **-**  검정 화면 (초 이후)  **설계 결함**   계산 오류 + 에셋 부족 핸들링 누락  -% (치명적)  **** 
 **-**  자막 배경/글씨 크기  구현 오류  - 상수   불일치  -점 (가독성)  **** 
 **-**  주제 무관 이미지 %  **데이터 품질**  키워드 매칭 알고리즘 + 기항지 에셋 부족  -.점 ( )  **** 
 **-**   타이밍 불일치  **설계 결함**    계산   타이밍 충돌  -.점 ( )  **** 

**총 영향** 등급 달성 불가 (점 목표 → 현재 -점, **-.점**)

---

## - 검정 화면 (초 이후)  

### . 증상
- 초 이후 비주얼이 검정 화면으로 전환
- 오디오는 정상 재생 ( 나레이션 들림)
-  -초 영상에서 마지막 -초가 검정색

### . 근본 원인 ( )

#### **설계 결함   계산 오류**

**위치** .-


#  
  (, (...)).()

#  -
 .  
    .(".이 입니다. 기본값 사용")
      .(..)  # .초 강제


**문제**
. 의 은 **자동 계산**(   )
. 하지만 의 일부가 **초 이전 종료**
. 남은 초는 **비주얼 없음** → 가 검정 화면으로 

**증거**

#  -  초과 시 트리밍만 있음
 .  ..
    # 트리밍 로직
 ..
     .  ..
        #  연장


→ **-초 구간에 에셋이 없을 때 처리 로직 누락**

---

#### **설계 결함  에셋 부족 시 핸들링 누락**

**위치** .- (- )


#  - 문장별 비주얼 매칭
 ,   ()
    # ... 비주얼 선택 로직
      .(, ..)

       
        #   실패 시  사용
        .(" 실패,  사용")
          ().(..)


**문제**
-  리스트가 **비어 있을 때** 처리 안 됨
- 빈 리스트 → () → **오류 또는  반환**
-  반환 → 에 추가 안 됨 → **검정 화면**

**재발 조건**
. / 세그먼트에서 키워드 매칭 실패
. 기항지 키워드 없는 세그먼트
. 후기 이미지 부족 ( 구간)

---

#### **설계 결함  /  불일치**

**위치** /.-


#  
   .  # -- .→.초 (단계 증폭)

#  
   .  # -- 로고 .초


**문제**

  초
  초
  초
실제 필요   () + () + () + ()  초

 실제 영상  초 → 초 


→ **초 구간을 어떻게 채울지 로직 없음**

---

### . 메모리 누수 위험 

**위치** .-


 (, , )
      

       .()
        # 개 자막  개 클립( + )  개 / 생성
          ((...), (,,)).().(.)
          (..., , ...)

        .()
        .()


**위험**
- 초 영상 → 약 개 자막 → **개  객체**
- 각 은 메모리에 **프레임 버퍼 유지**
- ..() 호출되지만, **해제 타이밍이 렌더링 완료 후**
- 렌더링 중 **메모리 누적** → +  사용

**증거**
-   메모리 안정성 패치에서  제한 ( , )
-   비활성화 이력 (메모리 절반 감소 목적, .)

**재발 방지 필요**
-   (필요 시점에만  생성)
-  풀 재사용 (같은 배경색 → 개  재사용)

---

### . 경계 조건 ( ) 미검증

**테스트 필요 케이스**

 케이스  현재 처리  위험 
-------------------------
 초 영상   초과 → 트리밍 ( )    
 줄 자막 (너무 짧음)   미달 →  연장 ( )    
 **에셋 개 (전체 실패)**   → **오류**   ** ** 
  실패 (무음)   → 무음 영상 ( )   경고만 
  세그먼트 없음   클립 생성 안 됨 → **초 **   ** ** 

**필수 검증 추가**

# 예상 코드 (미구현)
  
     ("에셋 매칭 개 - 영상 생성 불가")

 (      (, '')  .)  
     ("모든 클립이  - 렌더링 불가")


---

### . 에러 핸들링 부재

**위치** .-


  .(, .)

   
    .(" 실패,  사용")
      ().()


**문제**
.  → () → ****
.  발생 →  없음 → **프로그램 중단**
. 중단 시점이   (영상 조합) → **이미  완료 → 재실행 시 중복  호출**

**개선안**

   
      
        .("   비주얼 개 -  생성")
        #  검정 화면 + 텍스트
          ((, ), (,,), )
    
          ().()


---

### . 재발 방지 메커니즘

#### **  추가**


# /.
 ()
    """에셋 개 시  검증"""
      ()

    # 빈  전달
      .(
        ,
        "" "테스트", "" , "" ,
        ,
        "" ,
        
    )

    # 검정 화면  확인
        
     .  .  # 


#### **  추가**


 (,  ,  ) - 
    """최종 클립 리스트 검증"""
      
        .(" 클립 개 - 렌더링 불가")
         

      (
        ((, '', )  ) + (.  )
           
    )

       - 
       .
        .("   .초 -  또는  필요")
         

     


---

## - 자막 배경/글씨 크기 문제  

### . 증상
- 자막 배경이 너무 작음 (텍스트와 여백 불일치)
- 글씨 크기가 설정값과 다름 ( 기대 → 실제  렌더링)

### . 근본 원인

#### **구현 오류  - 상수**

**위치** .-


#  -  자막
 
        # -- → (양쪽 잘림 방지)
      
        '' ,  # ←  -
        '' (, )
    

#  -  자막

      
        '' ..,  # ←  
        '' (.., )  # 
    


**문제**
.  자막 **** (-)
.  자막 **** ()
.  메시지 **** ()

→ **일관성 없음 + 유지보수 어려움**

**증거**
- . →    
- . →    
- 코드는  사용 → **와  차이**

---

#### **구현 오류  배경 크기 계산 오류**

**위치** .-


#  -
 ..
    ,   .
       + .. *   #  *   
       + .. * 


**문제**
- .는 **렌더링 후 실제 크기**
- 하지만 '' + (, ) 지정 시 → **강제 크기 적용**
- 실제 텍스트가 인데, (, ) →  반환
- 배경 크기   +   **** (불필요하게 큼)

**증거**

#   
'' (, )  # 너비 고정, 높이 자동

#   
'' (.., )  #    고정


→ **실제 텍스트 길이와 무관하게 배경 크기 고정**

---

#### **설계 결함 폰트 크기    모순**

**위치** .-  .


# .
     # -- → (+%, 시니어 가독성)
     # --    - % 안전 마진

# .
    # -- → (양쪽 잘림 방지)


**모순**
- 는  권장 (시니어 가독성 우선)
- 코드는  강제 (  준수 우선)

→ **트레이드오프가 명시되지 않음**

**올바른 접근**
. **는 줄 자막 기준** (한 줄당  높이)
. **는 줄  기준** (한 줄만 사용)
. 하지만 코드는 이 구분을 **주석으로만 설명** → 설정값으로 분리 필요

---

### . 재발 방지

#### ** 통합**


# /. (수정 필요)
#  자막 (줄)
   
   
     # 신규

#  자막 (줄, 크기 우선)
     # 기존  →  (코드와 일치)
   


#### **배경 크기 자동 계산**


# 개선안 (실제 텍스트 크기 기준)
  (**)

# 실제 렌더링 크기 ( 강제 전)
  .  # '' 적용 전 크기

# 배경은 실제 크기 + 
   + .. * 
   + .. * 


---

## - 주제 무관 이미지 %  

### . 증상
- 기항지 대본 (예 나가사키) → 무관한 이미지 (예 홍콩, 선박 내부)
- 후기 세그먼트 → 맛집/풍경 이미지 (후기 이미지 %)
-    .초 / 목표 초 (**-.초**, -% )

### . 근본 원인

#### **데이터 품질  기항지 에셋 부족**

**위치** 크루즈정보사진정리

**현황**

일본 기항지 개 폴더 (나가사키, 후쿠오카, 오키나와 등)
그리스 기항지 개 폴더 (산토리니, 아테네, 미코노스, 로도스)
동유럽 기항지 개 (두브로브니크, 코토르, 스플릿 부재) ← 

총 ,장 중 기항지 이미지 약 장 (.%)


**문제**
-  는 "올인클루시브 가치형" → **동유럽 크루즈 추정**
- 하지만 동유럽 에셋 장 → **일본/그리스로 대체**
- 대체 과정에서 **키워드 매칭 실패** → 선박 내부 이미지 선택

**재발 조건**

대본 "두브로브니크의 성벽을 따라 걸으며..."
키워드 추출 "두브로브니크", "성벽"
에셋 검색 개 (폴더 없음)
   매칭 (동유럽 키워드)
  실패 →  매칭 (크루즈, 바다, 여행)
  랜덤 선택 →  선박 내부 이미지


---

#### **알고리즘 문제 키워드 매칭 점수 계산**

**위치** //. ()

**로직** (코드 미확인, 추정)

 ( ,  ) - 
      

    # . 정확한 매칭 (점)
       
           .()
             + 

    # . 유사도 매칭 (.)
       
          .(, , ).()
         +  * 

    # .  매칭 (보너스)
      .(, )
       
           
             +   #  - 

     (, )


**문제**
. **가 너무 범용적**
   - '크루즈', '', '바다', '여행' → % 이미지에 매칭
   - 점수 차이 미미 → **랜덤 선택과 유사**

. **기항지 키워드 우선순위 낮음**
   -  (개) 추가되었지만 ( -)
   - 키워드 부스트가 **. ()  . ()**
   - 기항지는  세그먼트 → **부스트 없음**

. ** 체인 부재**
   -   비활성화 (   , .)
   - 에셋 부족 시 **실시간 검색 안 함**

---

#### **설계 결함 후기 이미지 우선순위**

**위치** //.- ()


# - 후기 이미지  연동
  
    " 벨리시마 골든 파티",
    " 샴페인 바",
    # ... 프리미엄 이미지 개



**문제**
-  -에서 "후기이미지연동" 완료
- 하지만 ** 세그먼트에서만 우선 사용**
- / 세그먼트는 **여전히 랜덤 선택**

**증거**
- .   "후기 이미지 //에서 우선 사용"
- 하지만 코드는 **만 구현**

---

### . 재발 방지

#### **긴급 에셋 수집**

**  ** (.  )

  이미지 수집 (두브로브니크/코토르/스플릿)


**필요 작업**
.   활성화 (월 회 무료)
. 동유럽 대 기항지 각 장 다운로드
. 크루즈정보사진정리 폴더 생성

**예상 효과**
-    .초 → **초** (+%)
-   점수 점 → **점** (+점)

---

#### **알고리즘 개선**

**우선순위 가중치 조정**

# //. (수정 필요)
  
    "" 
        "" . → .,  # 기항지 매칭 강화
        "" .,   # 신규  키워드 추가 부스트
    


#  개선
   
     +   # 기항지 매칭 시 대폭 가산점


** 체인 추가**

# 순위 정확한 기항지 매칭
# 순위 같은 지역 매칭 (동유럽, 일본, 알래스카)
# 순위   실시간 검색
# 순위 선박 내부 프리미엄 이미지 (현재)


---

## -  타이밍 불일치  

### . 증상
- 목표 초, 초, 초
- 실제 초, 초, 초
-   / (타이밍 오차 허용 초 초과)
-   / (간격은 정상)

### . 근본 원인

#### **설계 결함    계산   타이밍 충돌**

**위치** .-


#  -
# - -- 스크립트에서  타이밍 우선 읽기
  
   .('', )
       .('', )
         ''  
            .('')

 
      ()  # 최대 개, 정렬
    .("- 스크립트에서  타이밍 읽음 ")

    #    기본값
      ., ., .
    .("- 스크립트에  타이밍 없음, 기본값 사용 ")


**문제**
. **가 생성한 **은 ** ** 기준
   
    (초) →  
    (초) →  
    (초) →    ←   (초 목표)
   

. **하지만 실제 렌더링**은 **- 삽입 후** 계산
   
    (초)
    (초)
   → - (.초) ← 삽입
    (초) → 실제 초 (초 지연)
   

. ** 타이밍은 스크립트 그대로 사용** (초)
   - 하지만 가 초로 이동
   - 이 초에 나타남 →  중간 (정확)
   - ** 등급 채점은 절대 시간 기준** → 초 ≠ 초 목표 (- 보정 안 됨)

---

#### **설계 결함  - 타이밍 보정 누락**

**위치** /. (등급 채점)

**채점 로직** (추정)

 ( ,  ) - 
      

     ,   (, )
         ( - )  .  # 초 허용
             + 

     ( / ) *   # 점 만점


**문제**
- - 삽입 시 **모든 후속 타이밍 +.초씩 지연**
-  타이밍은 **보정 안 됨**
- 채점 시 **절대 시간 비교** → 오차 누적

**예시**

목표 , , 
- 없을 때 ., ., . → / 
- 개 (초, 초) 삽입 후
  실제 ., ., . → /  (모두 초 초과)


---

#### **알고리즘 문제   모호성**

**위치** /. (-, )

** 프롬프트** (추정)

"" 
  
    "" "월 만원",
    "" .,  ←   기준
    "" .
  



**문제**
. 는 **- 존재를 모름** (프롬프트에 명시 안 됨)
. -은 **렌더링 단계에서 삽입** ()
.  타이밍 ≠ 최종 렌더링 타이밍

**올바른 접근**

# . 는 상대 타이밍만 생성 (세그먼트 내 위치)
"" 
  
    "" "월 만원",
    "" ,  #  세그먼트
    "" .,       #  시작 후 초
    "" .
  


# . 렌더링 시 절대 시간 계산 (- 반영)
   + .


---

### . 재발 방지

#### **타이밍 보정 시스템**


# . (추가 필요)
 (
     ,
     
) - 
    """- 삽입으로 인한  타이밍 보정"""
      

       
        # 이  이전에 삽입된 - 개수
          (.        )
        .( + )

     


#### **등급 채점 개선**


# /. (수정 필요)
 (
     ,
     ,
       # 신규 인자
) - 
    #  타이밍을 - 기준으로 보정
      
         + (.        )
           
    

      (
          ,   (, )
         ( - )  .
    )

     ( / ) * 


---

## 통합 수정 우선순위

 우선순위  버그   작업  시간  점수 효과   
----------------------------------------------
 ****  -  검정 화면  +   .  +% (렌더링 성공)  **무한대** 
 ****  -   타이밍 보정 시스템  .  +.점  .점/ 
 ****  -  자막  통합 + 배경 크기 수정  .  +점 (가독성)  점/ 
 ****  -  동유럽 에셋 수집 + 알고리즘 개선  .  +.점  .점/ 

**총 시간** .시간
**총 효과** 등급 달성 (점) + 렌더링 안정성 %

---

## 방어적 코딩 가이드라인

### . / 체크 (모든  생성 전)


# 
  ()
  .()

# 
  ().()
    .("파일 없음 ")
     

  ()
      (, '')
    .("손상된 파일 ")
     

  .()      


### . 리스트 연산 전  체크


# 
  ()

# 
  
    .("빈 클립 리스트 -  생성")
     ((, ), (,,), .)

  ()


### .  계산 후 검증


# 
  (, (, ))

# 
  (, (, ))

 .  
    .(" 계산 실패 - 기본값 적용")
      .(.)

 .  .
    .(" 너무 짧음 ..초")
     ("최소 초 이상 필요")


### . 메모리 안전 패턴


#  (개  동시 생성)
  
   
      (...)
      (...)
    .(, )

#  ( 사용)
 ()
       
          (...)
         

          (...)
         

  (())


### . 경계 조건  


# /.
..(",", 
    (., .),  #  초과 → 트리밍
    (., .),  #  미달 → 
    (., ),  # 초 → 오류
)
 (, )
     (, )  (, )
         .()
            .(..., )
    
          .(..., )
         (. - )  .


---

## 결론

### 버그  달성 체크리스트

-   **-**   추가 (에셋 개,  )
-   **-**    (검정 화면 방지)
-   **-**   추가 (.)
-   **-**  통합 ( )
-   **-** 배경 크기 동적 계산 (실제 텍스트 크기 기준)
-   **-** 동유럽 에셋 장 수집 ( )
-   **-** 키워드 부스트 조정 ( . → .)
-   **-**  타이밍 보정 함수 추가
-   **-** 등급 채점 - 반영

### 예상 효과 (수정 후)

 지표  현재  목표  개선 
------------------------
 등급 점수  점  점  +% 
 렌더링 성공률  %  %  +% 
 완주율  %  %  +% 
     .초  초  +% 
  타이밍 정확도  /  /  +% 

**최종 ** .시간 투자 → 월 수익 +만원 (+%)

---

**다음 단계**   착수 (- + - 우선 수정, .시간)
