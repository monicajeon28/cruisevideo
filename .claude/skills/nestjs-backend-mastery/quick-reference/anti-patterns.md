# -

  +  + 에서 피해야 할 패턴들

---

## . 타입 관련 안티패턴

###    사용


//  - 각 프로젝트에서 개별 정의
// ////..
()
(()         ) 

// ////.
  
   
   
     // 오타 가능 ''


// 문제점
// - 곳에서 동일한 타입 정의 → 유지보수 
// - 하나를 수정하면 다른 곳 누락 가능
// - 로 불일치 감지 불가



//  -  패키지에서 
  ,     '/'

// 
   () 

// 
   (
   (),
)


###  스키마와 타입 분리 정의


//  - 동기화 보장 안 됨
  
   
   
   


   .(
   .(),   // 타입과 불일치! (  )
   .(),
   .(),  // 타입과 불일치! (  )
)



//  - 스키마에서 타입 추론
   .(
   .().(),
   .().(),
   .().().(),
)

   . 
// →        


###   타입 사용


// 
()
 (()  ) 
   ..()


// 
     .()



// 
()
 (()  )  
   ..()


// 
   .( .())


---

## .   안티패턴

###   데코레이터 누락


//  - 문서화 불완전
('')
   
  ()
  (()  ) 
     ..()
  




//  - 완전한 문서화
('')
('')
('-')
   
  ()
  (  '사용자 생성' )
  (   )
  (  ,   )
  (  ,  '검증 실패' )
  (  ,  '이메일 중복' )
  (()  )  
     ..()
  



###  반환 타입 미명시


//  - 타입 추론에 의존
('')
 (('')  ) 
   ..()




//  - 명시적 반환 타입
('')
 (
  ('', )  ,
)  
   ..()



###  파이프 없이 파라미터 사용


//  - 런타임 검증 없음
('')
(('')  )   // 어떤 문자열이든 통과
   ..()


()
(
  ('')  ,      // ""도 통과
  ('')  ,
) 



//  - 로 검증
('')
(('', )  ) 
   ..()


()
(
  ('',  (), )  ,
  ('',  (), )  ,
) 


---

## .  안티패턴

###  런타임 검증 없이 타입만 사용


//  - 런타임 에러 발생 가능
  
   
   


    .()  // 검증 없음!
.(..())      // 가 이면 에러



//  - 로 런타임 검증
   .(
   .().(),
   .(),
)

   .(.())
 (!.) 
    (..())

   .  // 타입 안전 보장


###  중복 검증 (- + )


//  - 두 시스템 혼용
   
  ()           // -
   

  ()
  ()
   


// +  스키마 별도 관리
    .(
   .().(),
   .().(),
)



//  - 만 사용
// ////..
    .(
   .().(),
   .().(),
)

// /////-..
    () 


---

## . 모노레포 구조 안티패턴

###  역방향 의존성


//  - 가 에 의존
// ////.
     '/'  //  금지!



허용된 의존성 방향
/ → / 
/     → / 
/  → / 

금지된 의존성 방향
/ → /*       
/ → /        


###  에 프레임워크 코드 포함


//  -  의존성
// ////.
     '/'  // 

    (... ) 
  ('', )



//  - 순수 만
// ////.
    
   '',
   '',
   '-',
  

       


---

## .   안티패턴

###  수동 타입 정의


//  -  변경 시 수동 동기화 필요
// ////.
  
   
   
   


  
   
   
  // ... 백엔드와 다를 수 있음!




//  - 로 자동 생성
  ,     '/-'

   ()
// 는  스펙에서 자동 추론


###  / 직접 호출


//  - 타입 안전성 없음
    ('//', 
   '',
   .(),
)
    .()  //  타입



//  - 자동 생성된 훅 사용
   ()

    .(
   ,  // 타입 체크됨
)
// 는  타입으로 추론됨


---

## . 에러 처리 안티패턴

###   타입 단언 남용


//  - 타입 시스템 우회
    ..(     )  
..()  // 가 이면 에러!



//  - 타입 가드 사용
    ..(     )
 (!) 
    ( $  )

// 이 시점에서 는  타입 ( 아님)
..()  // 안전


###  외부 데이터 무검증 사용


//  - 외부  응답 신뢰
    .('/-/')
    .  // 실제 구조가 다르면



//  - 로 검증
   .(
   .(),
   .().(),
)

    .('/-/')
   .(.)

 (!.) 
    (' ', .)


   .  // 타입 안전


---

## . 요약 표

 안티패턴  문제점  해결책 
-------------------------
    유지보수    패키지 
 스키마/타입 분리  불일치  . 
  타입  타입 안전성 없음  명시적 타입 
  누락  문서화 불완전  데코레이터 필수 
 파이프 없음  런타임 검증 없음   
 중복 검증  복잡도 증가  만 사용 
 역방향 의존성  순환 참조  단방향 강제 
 수동 타입 정의  동기화 안 됨   자동 생성 
  타입 단언  타입 시스템 우회  타입 가드 

---

**** ..
