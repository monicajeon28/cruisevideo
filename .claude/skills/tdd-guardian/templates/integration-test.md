#   

##    


// ═══════════════════════════════════════════════════════════════
//   
//  /.-.
// ═══════════════════════════════════════════════════════════════

  ,    '/'
  ,    '/'
 *    ''
     '..//.'
     ''

('  ()', ()  
    
    

  // ─────────────────────────────────────────────────────────────
  //  
  // ─────────────────────────────────────────────────────────────

  ( ()  
         .(
       ,
    ).()

      .()
    .( (
       ,
       ,
    ))

     .()

      .()
  )

  ( ()  
     .()
  )

  ( ()  
    //     
     .('  ')
  )

  // ─────────────────────────────────────────────────────────────
  //  / 
  // ─────────────────────────────────────────────────────────────

  (' /', ()  
       
       '.',
       ' ',
       '!',
    

    //  
    ('      ()',  ()  
          (.())
        .('/')
        .()
        .()

      (.).(
         .(),
         '.',
         ' ',
         .(),
      )

      //      
      (..).()
    )

    //  
    ('    ()',  ()  
          (.())
        .('/')
        .( ...,  '-' )
        .()

      (..).('')
    )

    //  
    ('    ()',  ()  
      //   
       (.())
        .('/')
        .()
        .()

      //    
          (.())
        .('/')
        .()
        .()

      (..).(' ')
    )

    //   
    ('    ()',  ()  
        , ...   

          (.())
        .('/')
        .()
        .()

      (..).('')
    )
  )

  // ─────────────────────────────────────────────────────────────
  //  // 
  // ─────────────────────────────────────────────────────────────

  (' //', ()  
      

    ( ()  
          (.())
        .('/')
        .(
           '.',
           ' ',
           '!',
        )
        ..
    )

    //  
    ('     ()',  ()  
          (.())
        .(//$)
        .()

      (.).(
         ,
         '.',
         ' ',
      )
    )

    //  
    ('    - ',  ()  
          (.())
        .('//--')
        .()

      (..).(' ')
    )
  )

  // ─────────────────────────────────────────────────────────────
  //   
  // ─────────────────────────────────────────────────────────────

  ('  ', ()  
    ('    ',  ()  
      // . 
          (.())
        .('/')
        .(
           '.',
           ' ',
           '!',
        )
        .()

         ..

      // . 
          (.())
        .(//$)
        .()

      (..).(' ')

      // . 
          (.())
        .(//$)
        .(  ' ' )
        .()

      (..).(' ')

      // . 
       (.())
        .(//$)
        .()

      // .  
       (.())
        .(//$)
        .()
    )
  )
)


##    


// ═══════════════════════════════════════════════════════════════
//    
// ═══════════════════════════════════════════════════════════════

('  ()', ()  
    
    

  ( ()  
    // ...   ...

    //     
        (.())
      .('//')
      .(
         '.',
         '!',
      )

      ..
  )

  (' / ()', ()  
    ('    ',  ()  
       (.())
        .('/')
        .()
    )

    ('     ',  ()  
       (.())
        .('/')
        .('', ' -')
        .()
    )

    ('     ',  ()  
          (.())
        .('/')
        .('',  $)
        .()

      (.(.)).()
    )
  )
)

