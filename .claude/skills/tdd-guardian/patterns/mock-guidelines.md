#  

##        


# ═══════════════════════════════════════════════════════════════
#  
# ═══════════════════════════════════════════════════════════════


  
    - "  (외부 서비스)"
    - " ( 에서)"
    - " "
    - " "
    - " "
   "외부 의존성은 불안정하고 느림"


  
    - " "
    - " "
    - " "
    - " "
   "실제 비즈니스 로직이 테스트되어야 함"


   "테스트 대상 코드 외의 것만 "
   
    // 를 테스트할 때
    //    ( 접근)
    //    자체의  메서드


  - "모든 의존성이 됨"
  - "테스트가 구현을 미러링함"
  - "리팩토링하면 테스트가 깨짐"
  - "테스트 설정이 테스트보다 김"


##     


// ═══════════════════════════════════════════════════════════════
//  사용 예시
// ═══════════════════════════════════════════════════════════════

//   - (모든 것을 )
(' - ', ()  
  ('  ',  ()  
    // 모든 것을 
    .(, '').()
    .(, '').()
    .(, '').()
    .(, '').(   )
    .(, '').()
    .(, '').()

        .()

    //  이 테스트는 아무것도 테스트하지 않음
    // 모든 로직이 되어서 실제 비즈니스 로직 검증 불가
    (.).()
  )
)

//     (외부 의존성만 )
(' - ', ()  
  ('      ',  ()  
    // 외부 의존성만 
    .(, '').(   )
    .(, '').( 
      .( ...,  '-' )
    )

       
       
          '-',  ,   ,
          '-',  ,   ,
      ,
    

        .()

    //  실제 비즈니스 로직 검증
    (.).()  // 실제 계산 검증
    (.).()
  )

  ('     ',  ()  
    // 재고 부족 시나리오
    .(, '').()

     (.())
      ..()

    // 결제가 시도되지 않아야 함
    (.)..()
  )
)


##   


// ═══════════════════════════════════════════════════════════════
//  구현 패턴
// ═══════════════════════════════════════════════════════════════

//    
   (  )  (
   '-',
   '.',
   ' ',
  ...,
)

//    
   ()  (
   .(),
   .(),
   .(),
   .(),
   .(),
)

//       
   ()  (
   .().(()),
   .().(() 
    .(  '-', ... )
  ),
   .().((, ) 
    .( , ... )
  ),
   .().(),
)


## -    ' 


//   외부 라이브러리 내부 
.('', ()  (
  ....(''),
     ,  // 라이브러리 동작 변경
))

//    만들어서 
// //.
    (, )

// 
.('//', ()  (
   .(  ()),
))

